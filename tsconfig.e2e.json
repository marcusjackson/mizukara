{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* E2E tests use Playwright's expect, not Vitest's — no vitest/globals needed. */
    "types": ["node"]
  },
  "include": ["e2e/**/*.ts", "playwright.config.ts"],
  "exclude": ["node_modules", "dist"]
}
